<div class="">
  <div class="">
    <%= @picture.content %>
  </div>
  <div class="">
    <%= image_tag @picture.image.url %>
  </div>
    <%= link_to "edit", edit_picture_path(@picture.id) %>
    <%= link_to "destroy", picture_path(@picture.id), method: :delete %>
</div>
